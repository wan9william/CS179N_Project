using UnityEngine;
using Pathfinding;

public class AStarBootstrap : MonoBehaviour
{
    void Start()
    {
        AstarPath.active.Scan();
    }
    public void Scan()
    {
        AstarPath.active.Scan();
    }
}
